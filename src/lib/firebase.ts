// This file is kept to avoid breaking imports, but it is no longer used for Firebase functionality.
// All data is now handled via LocalStorage.

export const app = null;
export const auth = null;
export const db = null;
export const storage = null;
